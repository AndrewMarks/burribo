---
phase: 07-polish-deploy
plan: 01
type: execute
depends_on: []
files_modified: [assets/css/main.scss, _includes/breadcrumb.html, _layouts/post.html, _layouts/portfolio-visual.html, _layouts/portfolio-company.html]
---

<objective>
Add mobile responsive styles and breadcrumb navigation to the site.

Purpose: Ensure the portfolio looks great on mobile devices and inner pages have clear navigation back to homepage.
Output: Mobile-first responsive CSS, breadcrumb navigation for inner pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Current CSS:**
@assets/css/main.scss

**Key responsive concerns:**
- Hero section with large display type
- Navigation (currently horizontal)
- Card grids
- Portfolio layouts
- Blog typography (already has max-width constraints)

**Design system tokens to use:**
- --space-xs through --space-xl for spacing
- --text-hero, --text-display, --text-lg, --text-base, --text-sm for type scale
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responsive breakpoint variables and base mobile styles</name>
  <files>assets/css/main.scss</files>
  <action>
Add responsive breakpoints and mobile-first base adjustments to main.scss:

1. Add breakpoint variables to :root:
   ```scss
   --breakpoint-sm: 640px;   // Mobile landscape
   --breakpoint-md: 768px;   // Tablet
   --breakpoint-lg: 1024px;  // Desktop
   --breakpoint-xl: 1280px;  // Large desktop
   ```

2. Create responsive type scale adjustments:
   - Mobile: Reduce --text-hero to ~3rem, --text-display to ~2.5rem
   - Use clamp() for fluid typography where already defined

3. Add mobile header styles:
   - Smaller header height on mobile
   - Consider hamburger menu or simplified nav (can be basic for now)
   - Reduce nav gap and font sizes

4. Mobile section padding:
   - Reduce --space-xl to --space-lg on mobile
   - Reduce min-height on sections

Add styles at the end of main.scss in a clearly labeled responsive section.
  </action>
  <verify>Read file and confirm responsive section added with breakpoint variables</verify>
  <done>Breakpoint variables defined, mobile base styles added</done>
</task>

<task type="auto">
  <name>Task 2: Add responsive grid and layout styles</name>
  <files>assets/css/main.scss</files>
  <action>
Add responsive adjustments for grid layouts:

1. Card grid:
   - Mobile: Single column (1fr)
   - Tablet: 2 columns
   - Desktop: Auto-fit with minmax (existing)

2. Portfolio layouts:
   - Portfolio hero: Reduce padding, smaller title
   - Image gallery: 2 columns on tablet, 1 on mobile
   - Project cards: Stack vertically on mobile

3. Blog layout:
   - Already has max-width constraint, verify it works on mobile
   - Reduce padding on mobile

4. Company showcase:
   - Services grid: Single column on mobile
   - Company hero: Reduce padding and type sizes

Use @media (min-width: var(--breakpoint-md)) pattern for progressive enhancement.
  </action>
  <verify>Read file and confirm grid responsive styles added</verify>
  <done>All grid layouts respond properly to screen size</done>
</task>

<task type="auto">
  <name>Task 3: Add responsive navigation</name>
  <files>assets/css/main.scss</files>
  <action>
Add mobile navigation styles:

1. Mobile nav approach (choose simpler option):
   - Option A: Horizontal scroll with overflow-x: auto
   - Option B: Wrap to multiple lines with reduced spacing
   - Option C: Hide some links on mobile, show on desktop

   Recommend Option A or B for simplicity (no JS needed).

2. Nav styling on mobile:
   - Reduce gap between nav items
   - Smaller font size
   - Ensure touch targets are at least 44px

3. Logo/site-title:
   - Ensure it doesn't get cut off
   - Reduce margin-right on mobile

Keep it simple — no hamburger menu required unless navigation is very long.
  </action>
  <verify>Read file and confirm nav responsive styles added</verify>
  <done>Navigation works well on mobile with touch-friendly targets</done>
</task>

<task type="auto">
  <name>Task 4: Add breadcrumb navigation for inner pages</name>
  <files>_includes/breadcrumb.html, _layouts/post.html, _layouts/portfolio-visual.html, _layouts/portfolio-company.html, assets/css/main.scss</files>
  <action>
Add breadcrumb navigation so inner pages have clear navigation back to homepage:

1. Create `_includes/breadcrumb.html`:
   - Simple breadcrumb: Home > [Section] > [Page Title]
   - For blog posts: Home > Blog > [Post Title]
   - For portfolio: Home > [Category] > [Page Title]
   - Use semantic nav element with aria-label="Breadcrumb"
   - Link "Home" to root, section links to section pages

2. Add breadcrumb include to layouts:
   - `_layouts/post.html`: Add breadcrumb after opening body content
   - `_layouts/portfolio-visual.html`: Add breadcrumb above hero
   - `_layouts/portfolio-company.html`: Add breadcrumb above hero

3. Add breadcrumb styles to main.scss:
   - Positioned at top of content area
   - Muted color, small text (--text-sm)
   - Separator between items (> or /)
   - Links have subtle hover state
   - Responsive: stays readable on mobile

4. Breadcrumb structure:
   ```html
   <nav class="breadcrumb" aria-label="Breadcrumb">
     <ol>
       <li><a href="{{ '/' | relative_url }}">Home</a></li>
       <li><a href="{{ '/blog' | relative_url }}">Blog</a></li>
       <li aria-current="page">{{ page.title }}</li>
     </ol>
   </nav>
   ```
  </action>
  <verify>Read files and confirm breadcrumb added to all inner page layouts</verify>
  <done>Inner pages have clear navigation path back to homepage</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Breakpoint variables defined in :root
- [ ] Typography scales down on mobile
- [ ] Card grids stack properly
- [ ] Navigation is usable on mobile
- [ ] No horizontal scroll on mobile (except intentional)
- [ ] Breadcrumb navigation on all inner pages
</verification>

<success_criteria>
- Mobile-first responsive CSS added
- All major layouts work on mobile/tablet/desktop
- Typography remains readable at all sizes
- Touch targets are appropriately sized
- Inner pages have breadcrumb navigation back to homepage
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deploy/07-01-SUMMARY.md`

Commit per task:
- Task 1: feat(07-01): add responsive breakpoints and mobile base styles
- Task 2: feat(07-01): add responsive grid layouts
- Task 3: feat(07-01): add responsive navigation
- Task 4: feat(07-01): add breadcrumb navigation for inner pages
</output>
